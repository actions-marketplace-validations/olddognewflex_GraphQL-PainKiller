name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  build:
    name: Build CLI
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.22"
          cache: true

      - name: Build
        run: go build -o gql-painkiller ./cmd/gql-painkiller

  test:
    name: Unit Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.22"
          cache: true

      - name: Test
        run: go test ./...

  fixture-check:
    name: Painkiller Fixture Check
    runs-on: ubuntu-latest
    needs:
      - build
      - test
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.22"
          cache: true

      - name: Build CLI
        run: go build -o gql-painkiller ./cmd/gql-painkiller

      - name: Post summary comment to PR
        if: github.event_name == 'pull_request'
        run: ./gql-painkiller --fail-on=none post-pr-summary ./examples
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
